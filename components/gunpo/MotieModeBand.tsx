'use client'

/**
 * STEP12 — UNUSED.
 *
 * Previously: AX COUNCIL brand band + urban/people segmented toggle.
 * Reason unused: 군포 현안은 도시·정비와 시민·정주가 한 안건에 겹쳐 토글이
 * 오해를 만들고, STEP12에서 단일 모드로 통합했기 때문.
 *
 * Brand strip ("AX 군포") now lives in JejuThemeShell. Keep this file so a
 * future restore is a one-line re-import; do not render it.
 */

export function MotieModeBand() {
  return null
}
